const btnPlay = document.querySelector(".btnPlay")
const btnPause = document.querySelector(".btnPause")
const btnStop = document.querySelector(".btnStop")
const btnIncrease = document.querySelector(".btnIncrease")
const btnDecrease = document.querySelector(".btnDecrease")
const btnToggleSetTheMinutes = document.querySelector(".btnToggleSetTheMinutes")
const btnSetMinutes = document.querySelector(".btnSetTheMinutes")
const btnForest = document.querySelector("#btnForest")
const btnRain = document.querySelector("#btnRain")
const btnCoffeeShop = document.querySelector("#btnCoffeeShop")
const btnFireplace = document.querySelector("#btnFireplace")
const btnDarkMode = document.querySelector("#btnDarkMode")

const dialMinutes = document.querySelector(".minutes")
const dialSeconds = document.querySelector(".seconds")
const inputMinutes = document.querySelector("#inputMinutes")

const sliderForest = document.querySelector("#volume-control-Forest")
const sliderRain = document.querySelector("#volume-control-Rain")
const sliderCoffeeShop = document.querySelector("#volume-control-CoffeeShop")
const sliderFireplace = document.querySelector("#volume-control-Fireplace")

export const elements = {
    btnPlay,
    btnPause,
    btnStop,
    btnIncrease,
    btnDecrease,
    btnToggleSetTheMinutes,
    btnSetMinutes,
    btnForest,
    btnRain,
    btnCoffeeShop,
    btnFireplace,
    btnDarkMode,
    dialMinutes,
    dialSeconds,
    inputMinutes,
    sliderForest,
    sliderRain,
    sliderCoffeeShop,
    sliderFireplace
}